# Deprecated: Frontend Architecture

This file is no longer a source of truth.

Use `FRONTEND_SPEC.md` for canonical frontend routes, environment variables, API client contract, onboarding, dashboard, AI Astrologer UI, SEO pages, and build order.

Use `API_SPEC.md` for backend route contracts consumed by the frontend.

