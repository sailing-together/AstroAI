# Deprecated: Backend API

This file is no longer a source of truth.

Use `API_SPEC.md` for canonical backend routes, request/response shapes, auth rules, and live AI rules.

Use `ARCHITECTURE.md` for backend module boundaries, runtime configuration, and security requirements.

